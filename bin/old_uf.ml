(* with local lib (Conchon Filliatre) *)

(* open Utils *)

(* module UF = Uf_persistant.Make(Uf_persistant.A) *)
(* module PartitionAlgebra (P: sig val k : int end) = struct *)
(*   open P *)

(*   type t = UF.t * (int * int) list * UF.t *)

(*   let create() : UF.t = List.init k Int.succ |> List.map UF.create |> *)

(*   let id = create(), create() *)

(*   let concat (a, a': t) (b, b': t) = () *)

(* end *)


(* let () = print_endline "Hello, World!" *)
